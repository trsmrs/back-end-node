const {resolve} = require('path');

module.exports = {
    config: resolve(__dirname, 'src','app', 'configs', 'db.js'),
    'models-path': resolve(__dirname, 'src', 'apps', 'models'),
    'migrations-path': resolve(__dirname, 'src', 'app','database', 'migrations'),
}