const {resolve} = require('path');

module.exports = {
    congif: resolve(__dirname, 'src', 'config', 'db.js'),
    'models-path': resolve(__dirname, 'src', 'apps', 'models'),
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),
}